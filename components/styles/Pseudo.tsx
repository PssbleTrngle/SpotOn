import { css } from 'styled-components'

const Pseudo = css`
   content: '';
   position: absolute;
   top: 50%;
   left: 50%;
   transform: translate(-50%, -50%);
   width: 100%;
   height: 100%;
`

export default Pseudo
